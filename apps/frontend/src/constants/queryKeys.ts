export const QUERY_KEYS = {
  apiHealth: ["api-health"],
  clientRequests: ["client-requests"],
  myClientRequests: ["my-client-requests"],
  terminalKits: ["terminal-kits"],
  terminalKitsByUserId: ["terminal-kits-by-user"],
  terminalKitCapacity: ["terminal-kit-capacity"],
  terminalKitActionHistory: ["terminal-kit-action-history"],
} as const;
